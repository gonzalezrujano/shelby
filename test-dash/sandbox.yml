# Mock data for: shelby viz sandbox test-dash
# Each field is a list of values (oldest → newest).
# Timestamps are generated automatically.

pipelines:
  test-dash:
    my.field: [10, 20, 30, 40, 50]
